import React from 'react';
import {BrowserRouter,
    Switch,
    Route
} from 'react-router-dom';

const ToDoRouter = () => {
    return (
        <BrowserRouter>
            <Switch>
                <Route route='/' component={}></Route>
            </Switch>
        </BrowserRouter>
    );


}

